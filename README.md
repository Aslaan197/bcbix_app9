
  # BCBix

  This is a code bundle for BCBix. The original project is available at https://www.figma.com/design/AcENPppF0P5Rmi64tfFkVd/BCBix.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  